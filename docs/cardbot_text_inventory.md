# CardBot: текущие тексты бота

Статус: инвентаризация текущих текстов без правок.

Источник: `bot.py`, `payments.py`.

## Главное меню

### Сообщения

Триггер: `/start` или кнопка `🏠 Главная`.

```text
Здравствуйте, {first_name}.

Я помогу быстро подготовить карточку товара для WB и Ozon. У вас есть 3 бесплатные генерации.
```

Триггер: `/start`.

```text
Выберите действие:
```

### Кнопки

```text
⚡ Сгенерировать карточку
💳 Купить генерации
📊 Мой баланс
📋 Мои шаблоны
📋 История
❓ Помощь
🏠 Главная
```

## Генерация карточки

### Сообщения

Триггер: кнопка `⚡ Сгенерировать карточку` или команда `/generate`.

```text
Выберите маркетплейс:
```

Триггер: кнопка `Wildberries` или `Ozon`.

```text
Что сгенерировать?

«Только текст» — название, описание, ключевые слова, характеристики. Тратит 1 текстовую генерацию.
«Текст + изображения» — всё выше плюс изображения для карточки. Тратит 1 текстовую генерацию + N изображений.
```

Триггер: кнопка `📝 Только текст`.

```text
Отправьте описание товара. Минимум — название. Можно добавить материал, размер, цвет и особенности. Чем больше информации — тем лучше результат.
```

Триггер: пользователь отправил слишком короткое описание.

```text
Добавьте хотя бы название или категорию товара.
```

Триггер: начало текстовой генерации.

```text
⏳ Генерирую карточку...
```

Триггер: текстовая генерация успешно завершена.

```text
📌 НАЗВАНИЕ:
{title}
```

```text
📝 ОПИСАНИЕ:
{description}
```

Триггер: результат для Ozon.

```text
🔖 ХЭШТЕГИ:
{keywords}
```

```text
📋 ХАРАКТЕРИСТИКИ:
{characteristics}
```

Триггер: нет бесплатных или оплаченных текстовых генераций.

```text
Бесплатные генерации закончились.
```

Триггер: ошибка генерации.

```text
Произошла ошибка, попробуйте ещё раз. Генерация не списана.
```

Триггер: техническая ошибка сервиса или лимит внешнего API.

```text
Сейчас на стороне сервиса временные технические работы. Попробуйте немного позже. Генерация не списана.
```

Триггер: карточка создана, но история не сохранилась.

```text
Карточка создана, но не удалось сохранить историю. Попробуйте позже.
```

### Кнопки

Триггер: выбор маркетплейса.

```text
Wildberries
Ozon
🏠 Главная
```

Триггер: выбор режима генерации.

```text
📝 Только текст
📝🖼 Текст + изображения
⬅️ Назад
🏠 Главная
```

Триггер: после текстовой генерации.

```text
🔄 Сгенерировать ещё
💾 Сохранить как шаблон
🔄 Изменить и повторить
💳 Пополнить баланс
💬 Не понравился результат?
🏠 Главная
```

## Генерация текста и изображений

### Сообщения

Триггер: кнопка `📝🖼 Текст + изображения`, если на балансе есть изображения.

```text
Опишите товар: название, материал, размер, цвет, ключевые преимущества. Чем подробнее — тем лучше результат. После этого бот попросит загрузить фото товара.
```

Триггер: кнопка `📝🖼 Текст + изображения`, если на балансе нет изображений.

```text
У вас нет изображений на балансе.
Купите пакет изображений чтобы использовать этот режим.
```

Триггер: пользователь отправил описание для режима с изображениями.

```text
Загрузите от 1 до 7 фото товара с разных ракурсов. Когда загрузите все — нажмите ✅ Готово
```

Триггер: пользователь загрузил фото.

```text
Фото {added_count} получено ✓
Всего: {total_count}/7
```

Триггер: пользователь загрузил больше 7 фото.

```text
Максимум 7 фото. Нажмите ✅ Готово
```

Триггер: пользователь прислал не фото в шаге загрузки фото.

```text
Пришлите фото товара или изображение файлом.
```

Триггер: кнопка `✅ Готово`.

```text
Сколько изображений сгенерировать?

На вашем балансе: {image_balance} изображений
```

Триггер: начало генерации текста и изображений.

```text
⏳ Генерирую карточку...
```

Триггер: текстовая часть готова, изображения еще в работе.

```text
✅ Текстовая карточка готова:
```

```text
🖼 Изображения генерируются... подождите ~1-2 минуты
```

Триггер: прогресс генерации изображений.

```text
🎨 Генерирую изображения...
Сгенерировано: {generated_count}/{total_count}
Отправлено: {sent_count}/{total_count}
```

Триггер: прогресс генерации изображений, если запрос еще выполняется.

```text
🎨 Генерирую изображения...
Сгенерировано: {generated_count}/{total_count}
Отправлено: {sent_count}/{total_count}
Запрос еще выполняется, это может занять пару минут.
```

Триггер: подпись к отправленному изображению.

```text
Изображение {image_index}: {purpose}
```

Триггер: одно изображение не удалось сгенерировать.

```text
Изображение {next_to_send}/{total_count} не удалось сгенерировать.
```

Триггер: все изображения готовы.

```text
✅ Все изображения готовы!
Потрачено: 1 текстовая генерация + {generated_count} изображений
Остаток: {text_left} текстовых / {image_balance} изображений
```

Триггер: не хватает изображений на балансе.

```text
Не хватает {missing} изображений на балансе. Пополните баланс:
```

```text
Не хватает {missing} изображений на балансе.
```

Триггер: не удалось разработать концепцию изображений.

```text
Не удалось разработать концепцию изображений. Списана только текстовая генерация.
Остаток: {text_left} текстовых / {image_balance} изображений
```

Триггер: изображения отправлены, но история не сохранилась.

```text
Изображения отправлены, но не удалось сохранить историю. Баланс изображений не списан.
```

### Кнопки

Триггер: нет изображений на балансе.

```text
💳 Купить изображения
📝 Только текст
🏠 Главная
```

Триггер: загрузка фото.

```text
✅ Готово
📎 Ещё ({photos_count}/7)
🏠 Главная
```

Триггер: выбор количества изображений.

```text
1
3
5
7
🏠 Главная
```

Триггер: после генерации с изображениями.

```text
⚡ Сгенерировать ещё
💾 Сохранить как шаблон
🔄 Изменить и повторить
💳 Пополнить баланс
💬 Не понравился результат?
🏠 Главная
```

## Генерация только изображений

### Сообщения

Триггер: начало генерации только изображений.

```text
⏳ Разрабатываю концепцию изображений...
```

Триггер: сессия генерации изображений устарела.

```text
Сессия генерации изображений устарела. Начните заново.
```

Триггер: изображения готовы.

```text
✅ Готово! Сгенерировано {generated_count} изображений для карточки.
Остаток: {image_balance} изображений
```

Триггер: не удалось разработать концепцию изображений.

```text
Не удалось разработать концепцию изображений. Баланс не списан.
```

Триггер: изображения отправлены, но история не сохранилась.

```text
Изображения отправлены, но не удалось сохранить историю. Баланс не списан.
```

## Покупка и оплата

### Сообщения

Триггер: кнопка `💳 Купить генерации` или команда `/buy`.

```text
Что хотите купить?
```

Триггер: кнопка `Комбо: карточки + изображения`.

```text
Сколько карточек нужно в комбо-пакете?
```

Триггер: выбор количества карточек в комбо.

```text
{text_count} карточек: выберите количество фото
```

Триггер: кнопка `Только карточки`.

```text
Выберите пакет текстовых карточек:
```

Триггер: кнопка `Только изображения`.

```text
Выберите пакет изображений:
```

Триггер: выбран пакет оплаты.

```text
💳 Оплата пакета "{package_title}"
Сумма: {price_rub} ₽

После оплаты баланс пополнится автоматически.
```

Триггер: пакет оплаты не найден.

```text
Пакет не найден. Откройте /buy и выберите пакет заново.
```

Триггер: промо первой покупки уже использовано.

```text
Акция первой покупки уже использована.
```

Триггер: оплата не настроена или временно недоступна.

```text
💳 Оплата временно недоступна, скоро откроем!
```

### Кнопки

Триггер: меню покупки.

```text
Комбо: карточки + изображения
Только карточки
Только изображения
🏠 Главная
```

Триггер: выбор количества карточек в комбо.

```text
10 карточек
30 карточек
100 карточек
⬅️ Назад
🏠 Главная
```

Триггер: выбор количества фото на карточку в комбо.

```text
Без фото — {price_rub} ₽
3 фото/карточка — {price_rub} ₽
5 фото/карточка — {price_rub} ₽
7 фото/карточка — {price_rub} ₽
⬅️ Назад
🏠 Главная
```

Триггер: выбор пакета текстовых карточек.

```text
10 карточек за 560 ₽
30 карточек за 1 370 ₽
100 карточек за 3 440 ₽
⬅️ Назад
🏠 Главная
```

Триггер: выбор пакета изображений.

```text
20 изображений за 1 150 ₽
50 изображений за 2 750 ₽
150 изображений за 7 500 ₽
⬅️ Назад
🏠 Главная
```

Триггер: первая покупка изображений.

```text
10 изображений за 575 ₽
```

Триггер: ссылка на оплату.

```text
Перейти к оплате
🏠 Главная
```

### Названия пакетов

```text
Старт, без фото
Старт × 3 фото
Старт × 5 фото
Старт × 7 фото
Бизнес, без фото
Бизнес × 3 фото
Бизнес × 5 фото
Бизнес × 7 фото
Про, без фото
Про × 3 фото
Про × 5 фото
Про × 7 фото
Докупить 10 карточек
Докупить 30 карточек
Докупить 100 карточек
Докупить 20 изображений
Докупить 50 изображений
Докупить 150 изображений
Акция: 10 изображений
```

### Описания пакетов

```text
{text_count} карточек + {images_count} изображений
{text_count} карточек
{images_count} изображений
```

## Баланс

### Сообщения

Триггер: кнопка `📊 Мой баланс` или команда `/balance`.

```text
📊 Ваш баланс:

📝 Текстовых генераций: {text_balance}
🖼 Изображений: {image_balance}
```

### Кнопки

Триггер: экран баланса.

```text
Комбо: карточки + изображения
💳 Купить текстовые
💳 Купить изображения
🏠 Главная
```

## Шаблоны

### Сообщения

Триггер: кнопка `📋 Мои шаблоны` или команда `/templates`, если шаблонов нет.

```text
📋 У вас пока нет шаблонов.
```

Триггер: кнопка `📋 Мои шаблоны` или команда `/templates`, если шаблоны есть.

```text
📋 Ваши шаблоны ({total} из 10):

{index}. {template_name} ({marketplace}, {mode})
```

Триггер: выбор шаблона.

```text
📋 Шаблон: "{template_name}"
Маркетплейс: {marketplace}
Режим: {mode}
Описание: {description_preview}

Что сделать?
```

Триггер: кнопка `💾 Сохранить как шаблон`.

```text
Введите название шаблона.
Например: "Лампа спиральная" или "Коврик ЭВА"
```

Триггер: шаблон сохранен.

```text
✅ Шаблон "{name}" сохранён.
Найти его можно через /templates
```

Триггер: нет данных последней генерации для сохранения.

```text
Нет данных последней генерации для сохранения.
```

Триггер: достигнут лимит шаблонов.

```text
У вас уже 10 шаблонов — максимум. Удалите старый шаблон чтобы сохранить новый.
```

Триггер: кнопка `🗑 Удалить шаблон` в списке шаблонов.

```text
Выберите шаблон для удаления:
```

Триггер: кнопка удаления конкретного шаблона.

```text
Удалить шаблон "{template_name}"?
```

Триггер: шаблон удален.

```text
🗑 Шаблон удалён.
```

Триггер: удаление отменено.

```text
Удаление отменено.
```

Триггер: шаблон не найден.

```text
Шаблон не найден.
```

### Кнопки

Триггер: список шаблонов.

```text
{template_name}
← Назад
Вперёд →
🗑 Удалить шаблон
🏠 Главная
```

Триггер: список удаления шаблонов.

```text
{template_name}
← Назад
Вперёд →
❌ Отмена
🏠 Главная
```

Триггер: карточка шаблона.

```text
⚡ Использовать как есть
✏️ Изменить и использовать
🗑 Удалить шаблон
🏠 Главная
```

Триггер: подтверждение удаления шаблона.

```text
✅ Да, удалить
❌ Отмена
🏠 Главная
```

## Изменить и повторить

### Сообщения

Триггер: кнопка `🔄 Изменить и повторить`.

```text
Напишите только что изменилось.
Например: "цвет чёрный" или "размер XL, вес 2 кг"

Всё остальное останется как в предыдущей карточке.
```

Триггер: нет предыдущей генерации.

```text
Нет предыдущей генерации для повтора.
```

Триггер: режим повтора с изображениями.

```text
Использовать те же фото или загрузить новые?
```

Триггер: сессия повтора устарела.

```text
Сессия повтора устарела. Начните заново.
```

Триггер: в шаблоне нет сохраненных фото.

```text
В шаблоне нет сохранённых фото. Загрузите новые фото для генерации.
```

### Кнопки

Триггер: повтор с изображениями.

```text
📎 Те же фото
🖼 Загрузить новые
🏠 Главная
```

## История

### Сообщения

Триггер: кнопка `📋 История` или команда `/history`, если история пуста.

```text
История пока пустая.
```

Триггер: кнопка `📋 История` или команда `/history`, если есть генерации.

```text
📋 История #{index} — {created_at}

Запрос: {input_text}

📌 {output_title}

📝 {output_description}

🔑 {output_keywords}

📋 {output_characteristics}
```

## Обратная связь

### Сообщения

Триггер: кнопка `💬 Не понравился результат?`.

```text
Не понравился результат?

Напишите, что именно не так: название, описание, хэштеги или характеристики. Контакт для обратной связи: @alterega
```

## Помощь

### Сообщения

Триггер: кнопка `❓ Помощь` или команда `/help`.

```text
CardBot генерирует SEO-карточки товаров для Wildberries и Ozon.

Просто отправьте описание товара одним сообщением. Бот вернёт название, описание, ключевые слова и характеристики.

Контакт для вопросов по оплате и работе сервиса: alterega@list.ru
Самозанятый Дущенко Егор Владимирович
ИНН: 615422982815
Условия оказания услуги: публичная оферта.

/generate — создать карточку
/balance — баланс
/templates — мои шаблоны
/history — последние генерации
/buy — пакеты генераций
```

### Кнопки

Триггер: экран помощи.

```text
Публичная оферта
🏠 Главная
```

## Команды Telegram

```text
/start — Главное меню
/generate — Сгенерировать карточку
/balance — Показать баланс
/templates — Мои шаблоны
/history — Последние 5 генераций
/buy — Купить генерации
/help — Помощь
```
